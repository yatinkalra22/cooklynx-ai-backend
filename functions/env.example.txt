# Cloud Functions
CUSTOM_FUNCTION_REGION=us-central1

# Firebase (required for password verification)
WEB_API_KEY=

# Google OAuth (required for Google Sign-In)
GOOGLE_CLIENT_ID=

# Application
APP_URL=http://localhost:3000

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Default origins already include:
# - https://cooklynx-ai.firebaseapp.com (Firebase Hosting)
# - https://cooklynx-ai.web.app (Firebase Hosting)
# - https://cooklynx-ai.vercel.app (Vercel)
# Add additional origins here if needed
ALLOWED_ORIGINS=

# Gemini AI (required for image analysis)
GEMINI_API_KEY=
GEMINI_ENABLE_RETRY=false   # Set to "true" to enable retry logic for Gemini API calls

# Sentry
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1
SENTRY_ENVIRONMENT=
SENTRY_RELEASE=

# Image Upload
MAX_IMAGE_SIZE=10485760
ALLOWED_MIME_TYPES=image/jpeg,image/png,image/webp


# ===========================================
# Redis Configuration (Optional - disabled by default)
# ===========================================
# Set to "true" to enable Redis caching (requires Redis server)
# If disabled or Redis unavailable, app works normally with DB only
REDIS_ENABLED=false

# Redis connection settings (only used if REDIS_ENABLED=true)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Redis timeouts (milliseconds)
REDIS_CONNECT_TIMEOUT=2000
REDIS_COMMAND_TIMEOUT=1000

# ===========================================
# Cache TTL Configuration (seconds)
# ===========================================
# Only applies when Redis is enabled

# User data (short TTL - changes frequently)
CACHE_TTL_USER_CREDITS=300
CACHE_TTL_USER_PROFILE=600

# Image data
CACHE_TTL_IMAGE_METADATA=900
CACHE_TTL_IMAGE_LIST=300

# AI Analysis results (long TTL - immutable once complete)
CACHE_TTL_ANALYSIS=86400

# Fix results (long TTL - matches signed URL expiration)
CACHE_TTL_FIX_RESULT=604800
CACHE_TTL_FIX_INDEX=7200

# Image deduplication hash (very long TTL)
CACHE_TTL_IMAGE_HASH=2592000

# API response caching
CACHE_TTL_API_SHORT=300
CACHE_TTL_API_MEDIUM=900
CACHE_TTL_API_LONG=3600

 # Admin secret key for admin endpoints
ADMIN_SECRET_KEY=